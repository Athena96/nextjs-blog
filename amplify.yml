phases:
    preBuild:
      commands:
        - npm ci
build:
lommands:
        - npm run build
artifacts:
baseHirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
